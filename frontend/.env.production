# production build(GitHub Pages)打的後端網址 —— CloudFront HTTPS(見 infra/terraform/cloudfront.tf）。
# 不要改成裸 IP：GitHub Pages 是 HTTPS，瀏覽器會擋 HTTPS 頁面呼叫 HTTP API（mixed content）。
VITE_API_BASE_URL=https://db6vo5r7diqcz.cloudfront.net
